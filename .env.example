# 董事长专属智能工作台 — 环境变量模板
# 复制为 .env 后填入真实值（勿提交 .env）

# 模型提供方：openai（本地/兼容接口，默认） | claude（Claude Agent SDK）
LLM_PROVIDER=openai

# ---- OpenAI 兼容接口（Ollama / vLLM / LM Studio / OneAPI 等）----
# 注意：需带 /v1 前缀（最终请求 {BASE}/chat/completions）
OPENAI_BASE_URL=http://127.0.0.1:11434/v1
OPENAI_API_KEY=ollama
OPENAI_MODEL=qwen2.5:7b

# ---- Claude Agent SDK（仅 LLM_PROVIDER=claude 时需要）----
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ---- 汇讯 WiseUC（会议助手工具调用）----
# 默认模拟投递；实发时设 HUIXUN_ENABLED=true 并填写接口
HUIXUN_ENABLED=false
HUIXUN_BASE_URL=https://your-huixun-host
HUIXUN_APP_ID=
HUIXUN_APP_SECRET=
HUIXUN_SEND_PATH=/api/message/send

# 后端监听端口（Docker 内固定 3000）
PORT=3000

# ---- 本地 TTS（OmniVoice，docker compose 的 tts 服务）----
# 后端代理地址；Compose 内已设 TTS_URL=http://tts:8090
# TTS_URL=http://127.0.0.1:8090
# TTS_MODEL_ID=k2-fsa/OmniVoice
# TTS_MODEL_DIR=/data/models/OmniVoice
# TTS_DEVICE=auto
# TTS_VOICE_PROMPT=女，青年
# 国内下载权重：bash tts/download_model.sh（默认 ModelScope k2-fsa/OmniVoice）
# 改走 Hugging Face：TTS_DOWNLOAD_SOURCE=hf  HF_ENDPOINT=https://hf-mirror.com

# ---- 运行时数据（默认挂载项目 ./data → 容器 /data）----
# HRBP_REPORT_DIR=/data/hrbp-reports
